# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-startech-distro"
BBFILE_PATTERN_meta-startech-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-startech-distro = "6"

LAYERDEPENDS_meta-startech-distro = "core"
LAYERSERIES_COMPAT_meta-startech-distro = "scarthgap"
