# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-startech-bsp"
BBFILE_PATTERN_meta-startech-bsp = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-startech-bsp = "6"

LAYERDEPENDS_meta-startech-bsp = "core"
LAYERSERIES_COMPAT_meta-startech-bsp = "scarthgap"
